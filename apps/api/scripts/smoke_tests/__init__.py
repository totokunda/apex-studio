"""
Bundle smoke tests.

These are intended to be runnable:
- in-repo (development / CI),
- during bundling (executed by the shipped venv python),
- optionally from an installed bundle if copied into `apex-engine/scripts/`.
"""
