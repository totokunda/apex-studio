/**
 * Selection stroke color used for selection handles, borders, and accent UI
 * across preview clips (shape, text, video), timeline, and canvas components.
 * Change this value to update the selection visual style app-wide.
 */
export const SELECTION_STROKE_COLOR = "#ffffff";
